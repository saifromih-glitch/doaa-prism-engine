# Doaa Arabic Benchmark v1

## الغرض

يهدف هذا المعيار إلى قياس ما إذا كان Doaa يحقق هدفه الأساسي: تقليل ما يُرسل إلى النماذج المدفوعة عبر إعادة الاستخدام المحلي والبروتوكول المختصر، مع الحفاظ على الجودة والسلامة.

## مصادر بيانات مرشحة

يجب اختيار مجموعة بيانات مرجعية حقيقية ومراجعة ترخيصها قبل استخراج أي نتيجة عامة. من المرشحين **ArabicaQA**، وهي مجموعة أسئلة وأجوبة عربية منشورة بترخيص MIT وتضم تقسيمات تدريب وتحقق واختبار؛ وتظهر بطاقة البيانات أيضاً أن عارض البيانات يحتاج إلى تحديد حقل `data` في ملفات JSON، لذلك يجب بناء محمل مخصص والتحقق من سلامة الملفات قبل الاستخدام [1]. كما يمكن استخدام كتالوج **Masader** لاكتشاف مجموعات عربية إضافية حسب المجال واللهجة والمهمة، لكنه فهرس للمصادر وليس بديلاً عن مراجعة كل ترخيص وملف [2].

## المسارات المقارنة

يقارن المشغل بين `baseline`، و`doaa_local`، و`doaa_model_assisted`. لا يتم استنتاج جودة من عدد التوكنز وحده؛ يجب تسجيل الجودة، والهلوسة، والسلامة، والزمن، وفائدة المستخدم لكل حالة.

## المقاييس

| المقياس | التعريف |
|---|---|
| توفير التوكنز | الفرق النسبي بين إجمالي المسار الأساسي والمسار المقارن |
| الجودة | درجة مراجعة أو معيار محدد مسبقاً لكل حالة |
| معدل الهلوسة | نسبة النتائج التي تحتوي على ادعاء غير مدعوم وفق الملصق المعتمد |
| معدل السلامة | نسبة النتائج التي اجتازت بوابات السلامة |
| إعادة الاستخدام المحلي | نسبة الحالات التي حُسمت محلياً بتطابق محكوم |
| الزمن | زمن الاستجابة المسجل بالميلي ثانية |
| الفائدة البشرية | تقييم المستخدم، وهو ليس دليلاً على الحقيقة |

## أول تشغيل حقيقي للمسار المحلي

تم تشغيل 200 حالة حقيقية من `MRC/test` عبر `DoaaRuntime` باستخدام مكتبة محلية فارغة عمداً لقياس سلوك الحالات الجديدة. كانت جميع الحالات `route_model_or_review`، وكان معدل إعادة الاستخدام المحلي `0.0`، ولم يُستدعَ أي نموذج أو مصدر ويب. بلغ مجموع وحدات الإدخال التقديرية `160044` وفق مقياس `unicode_word_punctuation_proxy`. هذا يثبت أن Doaa لا يخترع تطابقات محلية للحالات الجديدة، لكنه لا يثبت جودة الإجابة أو توفير التوكنز؛ فالمكتبة الفارغة لا يمكنها الإجابة عن الأسئلة، ويجب تشغيل خط أساس ومحول نموذج صريح لإكمال المقارنة.

## قواعد النشر

الـ fixtures الصغيرة الموجودة في اختبارات Python تتحقق من صحة المشغل فقط ولا تنتج ادعاء أداء. لا يجوز نشر نسبة توفير أو جودة عامة إلا بعد تشغيل المشغل على بيانات حقيقية، ومراجعة الترخيص، وتوثيق نسخة البيانات، وتثبيت طريقة اختيار الحالات، وإظهار عدد الحالات المفقودة أو المستبعدة.

> نتيجة benchmark وصفية لنطاق ونسخة بيانات محددين؛ لا تعني ضماناً عاماً لكل النماذج أو اللغات أو المجالات.

## المراجع

[1]: https://huggingface.co/datasets/abdoelsayed/ArabicaQA "ArabicaQA dataset card"
[2]: https://arbml.github.io/masader/ "Masader Arabic NLP dataset catalogue"
