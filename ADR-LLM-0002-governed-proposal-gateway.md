# ADR-LLM-0002: بوابة اقتراح محلية محكومة

## القرار

تُضاف إلى دعاء طبقة اقتراح محلية تستخدم نموذج Ollama الموجود على الحاسوب لتحويل طلب منظم إلى **اقتراح DSL غير تنفيذي**. النموذج ليس منفذًا ولا يملك سلطة تعديل أو تشغيل. كل استجابة منه تُعامل كبيانات غير موثوقة، وتمر عبر متحقق حتمي مستقل قبل عرضها أو تمريرها إلى مراجعة بشرية.

## الحدود

لا تقرأ الطبقة ملفات أو أسرارًا، ولا تنفذ أوامر أو عمليات فرعية، ولا تفتح مقابس أو HTTP، ولا تصل إلى الشبكة، ولا تثبت أو تحدّث أو تنشر أو تدمج، ولا تعدل المصدر أو الاختبارات أو السجلات أو العقود. لا تُرسل بيانات حقيقية في التجربة الأولى؛ تُستخدم بيانات اصطناعية ووصف طلب محدود فقط.

يُحظر على المخرج أن يحمل سلطة تنفيذ أو ترقية أو كتابة. حتى الاقتراح المطابق لعملية مستقرة يبقى اقتراحًا يحتاج تحققًا وموافقة منفصلة. الهدف المجهول أو العملية غير المسجلة أو الحقل غير الموصوف يُرفض أو يتحول إلى طلب قدرة حوْكمي غير تنفيذي.

## نموذج التهديد

تُعامل المطالبة والنتيجة الخام وأي نص ينتجه النموذج باعتبارها مدخلات غير موثوقة قد تكون خاطئة أو مضللة أو غير مكتملة. الحماية المقصودة هي تقليل الصلاحيات، والتحقق الحتمي، وعدم تحويل اللغة الطبيعية إلى تنفيذ تلقائي. هذا القرار لا يدعي منع الهلوسة أو توفير أمن مطلق.

## الأصول المحمية

أصول دعاء البرمجية، مكتبة DSL والسجلات والعقود والاختبارات، ملفات المستخدم، الأسرار، إعدادات Windows، النماذج المحلية، وموارد الشبكة. لا تُمنح طبقة الاقتراح وصولًا مباشرًا إلى أي منها، عدا قناة JSON المحلية المحدودة اللازمة للطلب والنتيجة.

## معيار النجاح الأولي

يجب أن تنتج تجربة اصطناعية واحدة استجابة JSON صالحة أو رفضًا حتميًا، وأن يثبت المتحقق أن `execution_authority` يساوي `none`، وأن القدرات المحظورة غير مطلوبة، وأنه لا يوجد تنفيذ DSL أو كتابة مصدر أو اتصال شبكة. أي فشل في المخطط أو المهلة أو الحقول يؤدي إلى رفض آمن لا إلى إصلاح تخميني.

## القرار التالي

بعد نجاح النواة والاختبارات فقط يمكن تصميم واجهة استخدام أوسع. ويظل تشغيل البرنامج المقترح أو ترقية قدرة جديدة مسارًا منفصلًا يتطلب ADR وعقدًا وبوابات قبول جديدة.
