# ADR-DOAA-SEMANTIC-CHECKPOINT-0001

## القرار

يستخدم Doaa مرجعاً مختصراً قابلاً للعكس بدلاً من تلخيص دلالي غير مضمون. يُحفظ النص الأصلي محلياً دون تعديل، ويحتوي المرجع على بصمة checkpoint ومعرفات المقاطع والسؤال. في الجلسة الدافئة يمكن إرسال المرجع بدلاً من إعادة إرسال السياق الكامل، بشرط أن يكون checkpoint موجوداً ومطابق البصمة.

## سبب الاختيار

إرسال معرف إلى نموذج لا يملك checkpoint لا يكفي؛ لذلك يعلن العقد أن الجلسة الباردة تحتاج النص الأصلي، بينما تسمح الجلسة الدافئة بالمرجع المختصر. هذا يمنع افتراض ذاكرة غير موجودة لدى النموذج.

## نتيجة القياس

على 200 حالة حقيقية من ArabicaQA، كان المرجع المختصر أصغر في المتوسط بنسبة 74.0363% من السياق، واستُعيدت الحالات الـ200 بلا فقد عند توسيع جميع المقاطع. هذا قياس ضغط مرجع وتخزين، وليس إثباتاً أن النموذج يستطيع الإجابة من hash وحده.

## الحوكمة

المحتوى الأصلي غير قابل للتعديل عبر checkpoint، والمرجع المجهول أو المقطع المجهول أو البصمة غير المطابقة يُحجب. لا توجد ترقية معرفة أو تنفيذ أو إعادة محاولة تلقائية. يظل الفاحص الدلالي خارج هذا المكوّن؛ فالـcheckpoint يضمن القابلية للعكس، ولا يثبت صحة المصدر أو الإجابة.
