# الإدخال الخارجي والإخراج الآمن — القرار 0001

## النتيجة

وُسعت حزمة التشغيل الآمن للسماح بقراءة ملف CSV خارجي يحدده المستخدم، مع إبقاء الإخراج داخل مجلد Doaa-Local الآمن فقط. لا يمكن للحزمة الكتابة فوق ملف الإدخال، ولا إنشاء إخراج خارج الجذر المعتمد، ولا متابعة العملية إذا غاب عمود `phone`.

## الاختبارات

| الحالة | النتيجة |
|---|---|
| إدخال اختبار خارجي مناسب | نجح إلى إخراج جديد |
| الإدخال والإخراج متساويان | حُجب |
| بصمة اقتراح خاطئة | حُجب |
| ملف Downloads المختار بلا عمود `phone` | حُجب بـ`target_column_missing` |
| إنشاء إخراج للملف غير المناسب | لم يحدث |
| تعديل الأصل | لم يحدث |

اجتازت الحزمة أربعة اختبارات، ولم تُستخدم بيانات المستخدم في التحويل. أُجري على ملف Downloads فحص قراءة محدود، ثم أوقفه النظام لأن الملف جدول فنادق ولا يحتوي العمود المطلوب.

## السياسة

وجود نسخة احتياطية لا يلغي ضرورة التحقق من ملاءمة الملف. لذلك لا يكفي اختيار أي ملف؛ يجب أن يحتوي الملف على `phone` أو أن يُعتمد لاحقًا عقد منفصل للتحويل العربي `الهاتف`. لا يجوز للحزمة تخمين أن عمودًا آخر يمثل رقم الهاتف.

> الإدخال الخارجي مسموح بالقراءة فقط؛ الإخراج الجديد هو المسار الوحيد المسموح بالكتابة.

## القرار التالي

الحزمة جاهزة لاستقبال ملف CSV حقيقي مناسب عندما يحدد المستخدم ملفًا يحتوي بوضوح على عمود `phone`، مع إخراج جديد ومسار معتمد. لا تزال ملفات Excel والعناوين العربية خارج عقد التنفيذ الحالي حتى إضافة دعم صريح واختباره.
