# قرار عزل Ollama على CPU رقم 0001

## النطاق

جرت تجربة واحدة فقط على خادم Ollama مؤقت محلي مرتبط بـ`127.0.0.1:11435`. استخدمت العملية النموذج الموجود `qwen3.5:4b`، وسياقًا قدره 2048 ومخرجًا محدودًا، ولم تُرسل بيانات حقيقية ولم يُنفذ أي DSL.

## الدليل

| البند | النتيجة |
|---|---|
| تحرير التطبيقات المصرح بها | تحقق مباشرة؛ أصبحت الذاكرة الحرة نحو 5.43 GiB قبل تشغيل الخادم |
| خادم العزل المؤقت | أصبح جاهزًا محليًا، والنموذج `qwen3.5:4b` كان حاضرًا |
| طلب الاختبار الاصطناعي | اكتمل خلال 18.09 ثانية بلا استثناء من عميل API، لكن حقل `response` كان فارغًا ولم يحتوِ `CPU_SMOKE_OK` |
| سجل الخادم | يدل على تحميل النموذج ومعالجة 26 رمزًا؛ لا يُعامل ذلك كنجاح للعقد لأن الاستجابة المقبولة لم تظهر في الحقل المتفق عليه |
| تنفيذ DSL أو شبكة خارجية | لم يحدث |
| تنظيف الخادم | أغلق المنفذ 11435، وخادم Ollama الافتراضي لا يعرض نموذجًا نشطًا |

## القرار

لا يُدمج Ollama في دعاء الآن. أثبتت التجربة أن الخادم المحلي يستطيع البدء وتحميل نموذج في مسار العزل، لكنها لم تثبت أن واجهة الاستجابة المضبوطة تحقق مخرجًا قابلاً للقبول. كما أن خطأ NVML ما زال قائمًا بعد تحديث تعريف NVIDIA 610.88 وإعادة التشغيل، رغم أن Windows يعرض جهاز RTX 4050 وحالة تعريفه على أنها سليمة.

الخطوة الصحيحة التالية، إن فُوضت لاحقًا، هي عقد CPU ثانٍ منفصل يلتقط استجابة API الخام ويضبط سلوك التفكير صراحةً، ثم يحاول مرة واحدة فقط؛ وليس إعادة نفس تجربة 0001 أو تنفيذ أي اقتراح نموذج داخل دعاء.

> لا يثبت هذا التقرير تعطل Ollama أو سلامة GPU أو جودة النموذج. إنه يسجل حدود الأدلة المحلية الحالية فقط.
