# وضع الاستخدام اليومي وعملية توحيد الفراغات — القرار 0001

## النتيجة

أضيفت عملية DSL ثانية باسم `normalize_ascii_spaces`. تطبق العملية توحيد runs الفراغات ASCII وحذف الفراغات الطرفية في عمود واحد يحدده الطلب صراحةً. لا تغيّر الأعمدة الأخرى، ولا تتعامل مع الفراغات غير ASCII، ولا تستبدل الملف الأصلي.

كما تم تحديث نقطة الدخول `doaa_local.py` لتوجيه العملية الجديدة إلى منفذها الحتمي، مع إبقاء التشخيص ومسار الإخراج الجديد والحجب عند فشل الشروط.

## الاختبارات

| الاختبار | النتيجة |
|---|---|
| توحيد الفراغات في عمود `name` | نجح |
| الحفاظ على `phone` و`amount` | تحقق |
| اختلاف بصمة الاقتراح | حُجب |
| نقطة الدخول المحلية | نجحت |
| التشخيص بعد تحديث manifest | `diagnostics_passed` |
| التنفيذ التلقائي | غير موجود |
| الاتصال الخارجي | لم يحدث |

## مثال الناتج

الإدخال:

```csv
name,phone,amount
  Ali   Hassan  ,010-123 456,100
Mona  Salem,011 222-333,250
```

الناتج:

```csv
name,phone,amount
Ali Hassan,010-123 456,100
Mona Salem,011 222-333,250
```

## حدود الاستخدام اليومي

يجب تمرير طلب JSON واحد إلى `doaa_local.py`، ويجب أن يتضمن الاقتراح والمراجعة البشرية والبصمة ومسار الإدخال ومسار إخراج جديدًا. لا يوجد اختيار تلقائي للعمود، ولا تنفيذ دون الموافقة، ولا كتابة فوق الأصل.

> إضافة قدرة جديدة لا توسع سلطة النموذج؛ إنها تضيف مسارًا حتميًا جديدًا خلف عقد واختبارات.

## قرار الاعتماد

تم اعتماد العملية الثانية ضمن نطاق MVP المحلي، وأُعيد بناء ملف ZIP ليشمل العقد والمنفذ والاختبارات والدليل المحدث. أي عملية ثالثة تحتاج دورة مماثلة من العقد والتنفيذ والاختبار والتوثيق.
