# ADR-DOAA-COMMAND-LANGUAGE-0001

## القرار

تعتمد Doaa في الإصدار `doaa.command.v1` صيغة أوامر صريحة من الشكل `@library.template key=value`. لا يُسمح بتحويل اللغة الطبيعية إلى أمر من خلال التخمين؛ يجب أن يطابق اسم المكتبة والقالب السجل المجمد حرفياً.

## السبب

تقلل الصيغة المنظمة الغموض وتسمح بتحويل الطلب إلى بيانات قابلة للتحقق قبل الوصول إلى Runtime. وهي لا تجعل النموذج اللغوي منفذاً ولا تمنح Doaa سلطة تنفيذية.

## الحدود

المحلل لا يستدعي نموذجاً، ولا يصل إلى الشبكة أو الملفات أو الأسرار، ولا ينفذ أوامر، ولا ينشئ قوالب جديدة، ولا يحدّث المكتبات. القالب غير المسجل ينتج `governed_capability_request` فقط، ويحتاج عقداً واختبارات ومراجعة بشرية.

## المخاطر المخففة

يرفض المحلل المفاتيح غير المعروفة، والقيم المفقودة، والمفاتيح المكررة، والاقتباس غير الصحيح، والقيم الطويلة، ورموز التحكم الشائعة. كما يثبت كل مخرج على `execution_authority: none` و`automatic_execution: false`.

## غير المشمول

لا يثبت هذا القرار التكافؤ الدلالي مع اللغة الطبيعية، ولا يثبت منع جميع أشكال إساءة الاستخدام، ولا يضيف بحثاً حياً أو تحديثاً ذاتياً. هذه القدرات تحتاج عقوداً ومراجعات منفصلة.
