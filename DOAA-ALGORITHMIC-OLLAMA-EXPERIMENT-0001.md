# تقرير تجربة الوساطة الخوارزمية مع Ollama

## الحالة

تجربة فعلية محلية على نموذج `rabie-udda-cs:latest` عبر `http://127.0.0.1:11434/api/generate`، بتاريخ 2026-08-27. شملت التجربة ثلاث حالات عربية متقاربة، وقارنت مطالبة طبيعية مباشرة برسالة `doaa.alg.v1` تحمل خوارزمية `answer.summarize.v1`.

## النتيجة المختصرة

التجربة تثبت أن الاتصال المحلي يعمل وأن النموذج استطاع إنتاج إجابات عربية، لكنها **لا تثبت توفير التوكنات في التصميم الحالي**. في الحالات الثلاث استهلكت الرسائل الخوارزمية 3,143 prompt tokens مقابل 2,795 للمطالبات الطبيعية، أي زيادة قدرها 348 token تقريبًا أو 12.45%. السبب أن الرسالة الخوارزمية الحالية تحمل JSON كاملًا وسياق تعليم البروتوكول، بينما المطالبة الطبيعية قصيرة.

كما أن النموذج لم يلتزم بعقد النتيجة في أول حالة: أعاد `algorithm` كسلسلة بدل كائن، وأعاد `status: success` بدل الحالة المجمدة، ولم يكمل الرسالة بالشكل المطلوب. لذلك كان يجب أن تُرفض النتيجة عبر المتحقق، ولا يجوز اعتبار الإجابة قبولًا بروتوكوليًا تلقائيًا.

## الأرقام الفعلية من Ollama

| المقياس | المطالبات الطبيعية | الرسائل الخوارزمية |
|---|---:|---:|
| إجمالي prompt tokens | 2,795 | 3,143 |
| إجمالي output tokens | 109 | 340 |
| إجمالي زمن القياس الظاهر | 24,284.16 ms | 9,535.49 ms |
| متوسط الزمن للحالتين الدافئتين 2 و3 | 1,554.41 ms | 2,949.05 ms |
| عدد الحالات | 3 | 3 |

أول مطالبة طبيعية تحمل أثر تحميل النموذج، لذلك لا تصلح مقارنة زمنها بالحالة الخوارزمية الأولى وحدها. في المتوسط الدافئ للحالتين الثانية والثالثة كانت الرسالة الخوارزمية أبطأ، كما أنتجت مخرجات أطول بسبب طلب JSON وتعليم البروتوكول.

## فحص الجودة والالتزام

الحالات الثلاث أعادت نصًا عربيًا غير فارغ. لكن هذا لا يكفي لإثبات التكافؤ الدلالي. الحالة الأولى أعادت ملخصًا مقبولًا لغويًا تقريبًا لكنها خالفت عقد النتيجة. الحالتان الثانية والثالثة أعادتا بنية أقرب إلى العقد، مع استخدام `status: success` و`algorithm` كسلسلة، ولذلك تحتاجان إلى رفض أو إصلاح محكوم قبل العرض النهائي.

| فحص | النتيجة |
|---|---|
| Ollama محلي | ناجح |
| الاستجابة غير فارغة | 3 من 3 |
| الالتزام الكامل بعقد `doaa.alg.v1` | 0 من 3 مثبتة |
| تحقق الهوية والسلطة | الحقول الظاهرة تعلن `none` |
| تنفيذ أو أدوات | لم تُستخدم |
| إرسال خارجي | لم يحدث |

## القرار

لا نعتمد ادعاء أن دعاء يوفر التوكنات بعد هذه التجربة. القياس الحالي يوضح أن **التغليف JSON وحده ليس ضغطًا**، وأن تعليم البروتوكول قد يضيف تكلفة أكبر من الوفر. كما لا نعتمد مخرجات النموذج ما لم تمر بعقد النتيجة؛ فالاستجابة الخام تبقى غير موثوقة.

## التعديلات المطلوبة قبل التجربة التالية

يجب فصل تعليم البروتوكول الثابت عن كل رسالة أو تقليله إلى system prompt مثبت، واستخدام رمز أقصر ومعلمات أقل، ثم قياس prompt tokens مع وبدون تكلفة التعليم الثابت. يجب أيضًا إضافة محاولات تحقق محدودة لا تتجاوز إعادة طلب بنية JSON عند الفشل، دون تصحيح دلالي أو تنفيذ تلقائي. ويجب استخدام عداد الجودة مستقل يراجع وجود المعنى المطلوب، لا مجرد عدم فراغ النص.

## ما لا تثبته التجربة

لا تثبت هذه العينة الصغيرة توفيرًا عامًا أو جودة عامة أو تفوقًا على كل النماذج. لا تثبت التقطير أو التعلم أو قدرة الوكيل. النموذج المستخدم محلي ومحدد، والعدادات خاصة بـOllama، والمقارنة لا تزال أولية.
