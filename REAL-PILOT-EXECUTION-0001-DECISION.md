# تجربة التشغيل المنضبطة — القرار 0001

## الملف والعملية

استخدمت Doaa الملف الاصطناعي الذي وضعه المستخدم في Downloads:

```text
C:\Users\saifr\Downloads\Doaa-phone-test.csv
```

العملية المعتمدة هي `remove_ascii_phone_separators` على عمود `phone` فقط. أُنشئ الناتج في:

```text
C:\Users\saifr\OneDrive\Desktop\Doaa-Local\test-runs-pilot\Doaa-phone-test-pilot-output.csv
```

## النتيجة

| الفحص | النتيجة |
|---|---|
| التشخيص قبل التشغيل | `diagnostics_passed` |
| نوع الإدخال | CSV |
| العمود المستهدف | `phone` فقط |
| الصفوف قبل/بعد | 3 / 3 |
| الأعمدة قبل/بعد | `name`, `phone`, `amount` / نفسها |
| القيم المتغيرة | 3 قيم في `phone` |
| `name` و`amount` | لم يتغيرا |
| الأصل | لم يُعدل |
| الإخراج | ملف جديد |
| اتصال خارجي | لم يحدث |
| سلطة النموذج | `none` |
| الحالة | `local_flow_completed` / `executed_safe_file` |

## الناتج

```csv
name,phone,amount
Test Customer One,010123456,100
Test Customer Two,011222333,250
Test Customer Three,012444555,375
```

## القرار

نجحت التجربة التشغيلية المنضبطة. لم تُستخدم بيانات شخصية أو تجارية؛ الملف اصطناعي ومخصص للاختبار. بقي الأصل محفوظًا، وطبقت Doaa التحويل على العمود المحدد فقط.

> نجاح التجربة يثبت النطاق المختبر، ولا يثبت صلاحية معالجة أي ملف آخر دون فحص مخططه وموافقته ومسار إخراجه.

## الخطوة اللاحقة

يمكن الآن الانتقال إلى إعداد استخدام يومي أبسط، أو إضافة عمليات DSL جديدة واحدة في كل مرة. لا يُنصح بتوسيع الصلاحيات أو تفعيل التنفيذ التلقائي.
