# منفذ DSL التجريبي — القرار 0001

## النتيجة

تم تنفيذ منفذ مستقل بوضع `dry_run_only`. المنفذ لا يكتب ملف الإدخال أو الإخراج، ولا يعدل المصدر، ولا يتصل بالشبكة، بل يحسب معاينة في الذاكرة ويصدر إيصالًا بالنتيجة.

## الاختبارات

| الاختبار | النتيجة |
|---|---|
| إزالة المسافات والشرطات من `phone` | نجحت المعاينة |
| إبقاء `name` دون تغيير | تحقق |
| إبقاء `amount` دون تغيير | تحقق |
| إبقاء عدد الصفوف والأعمدة | تحقق |
| عدّ الخلايا المتغيرة | تحقق؛ خليتان في بيانات الاختبار |
| غياب الموافقة البشرية | حُجب التشغيل |
| كتابة المصدر أو الاتصال الخارجي | لم يحدث |

حزمة الانحدار الكاملة اجتازت: 5 اختبارات للمصحح، واختبار سجل التدقيق، و4 اختبارات للمراجعة البشرية، واختبارين للـDry Run.

## حدود النتيجة

هذه معاينة حسابية وليست تنفيذًا على ملف حقيقي. لا يجوز تفسير `dry_run_preview` على أنه تصريح كتابة. أي تنفيذ اختباري لاحق يحتاج عقدًا منفصلًا، ومسار موافقة صريحًا، وملف اختبار يحدده المستخدم أو يُنشأ بوضوح على أنه اختبار.

> المنفذ الحتمي ينفذ القاعدة المسجلة فقط؛ النموذج لا يدخل إلى مسار التحويل ولا يستطيع تغيير العمود المستهدف أو الأعمدة المحمية.

## شروط المسار التالي

قبل التنفيذ الاختباري الصريح يجب ربط هوية الاقتراح بسجل التدقيق وقرار المراجعة البشرية، وإضافة قفل يمنع الكتابة إلى أي مسار غير معتمد، ثم مقارنة قبل/بعد والتحقق من عدم تغير الأعمدة غير المستهدفة. لا يُسمح بعدُ باستخدام بيانات المستخدم الحقيقية.
