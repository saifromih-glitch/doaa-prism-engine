# Doaa Governed MVP — الحزمة المحلية 0001

## النتيجة

تم تغليف نواة Doaa الموحدة في نقطة دخول محلية واحدة هي `doaa_local.py`. تبدأ الأداة بالتشخيص، ثم تختار منفذ CSV أو XLSX، ولا تسمح بالتنفيذ إلا بعد تحقق البصمة والمراجعة البشرية ومسار الإخراج الجديد.

أُنشئ ملف ZIP نظيف على جهاز Windows في:

```text
C:\Users\saifr\OneDrive\Desktop\Doaa-Local\release\DOAA-GOVERNED-MVP-LOCAL-0001.zip
```

حجم الملف الذي جرى التحقق منه: 24,756 بايت.

## التحقق النهائي

| الفحص | النتيجة |
|---|---|
| اختبار الإصلاح المحدود | ناجح |
| اختبار سجل التدقيق | ناجح |
| اختبار المراجعة البشرية | ناجح |
| اختبار Dry Run | ناجح |
| اختبار CSV الآمن | ناجح |
| اختبار XLSX والعمود العربي | ناجح |
| اختبار المسار الموحد | ناجح |
| اختبار نقطة الدخول المحلية | ناجح |
| التشخيص الأمني | `diagnostics_passed` |
| إعادة التشغيل فوق إخراج موجود | محجوبة |
| تنفيذ تلقائي للنموذج | غير موجود |
| اتصال خارجي أثناء الاختبارات | لم يحدث |
| استخدام بيانات حقيقية في اختبارات الحزمة | لم يحدث |

## نطاق الإصدار

يدعم الإصدار CSV بعمود `phone`، وXLSX بعمود `phone` أو `الهاتف`، مع إزالة المسافات والشرطات ASCII من العمود المحدد فقط. يكتب الناتج إلى ملف جديد ولا يستبدل الأصل. لا يشمل الإصدار البحث على الويب، أو التحديث الذاتي، أو تثبيت الحزم، أو الوصول إلى الأسرار، أو تشغيل Ollama تلقائيًا.

> هذه الحزمة أداة محلية حوْكمية مقيدة، وليست وكيلًا مستقلًا. النموذج يقترح فقط، والبوابة والمراجعة والمنفذ الحتمي تظل طبقات منفصلة.

## طريقة البدء

فك ضغط الملف، ثم اقرأ `DOAA-LOCAL-README.md`. شغّل نقطة الدخول عبر طلب JSON واحد باستخدام PowerShell، مع تحديد ملف الإدخال ومسار إخراج جديد و`allowed_root`. يجب تحديد اسم الورقة صراحةً عند استخدام XLSX.

## قرار الاعتماد

الحزمة جاهزة كـ **Doaa Governed MVP Local 0001** ضمن النطاق المذكور. أي عملية جديدة أو صلاحية جديدة تحتاج عقدًا مستقلًا واختبارات وفحصًا أمنيًا قبل إضافتها.
