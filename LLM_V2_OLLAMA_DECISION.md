# قرار تجربة Ollama V2

**الحالة:** لا تكامل للنموذج المحلي في دعاء في هذه المرحلة.

## ما تم إثباته

| بند | النتيجة المثبتة |
|---|---|
| النموذج | `qwen3.5:4b` كان مثبتًا محليًا قبل التجربة |
| جاهزية الموارد قبل التشغيل | مقبولة: 6.97 GiB ذاكرة حرة، ولا نموذج Ollama نشط |
| عقد التجربة | `CONTRACT-LLM-0002`؛ طلب واحد فقط، مخرج صغير، وبيانات اصطناعية |
| المهلة | 300 ثانية |
| الاستجابة | فارغة عند انتهاء المهلة |
| تنفيذ DSL | لم يحدث |
| تعديل مصدر دعاء | لم يحدث |
| قرار المتحقق المستقل | `reject_integration_no_retry_under_this_contract` |

استغرقت العملية 300541 مللي ثانية ثم انتهت بالمهلة. بعد المحاولة ظهر النموذج محمّلًا في Ollama بحجم معروض 13 GB واستعمال مختلط CPU/GPU، لكن ذلك لا يثبت أن النموذج معطّل أو أن Ollama لا يعمل. يثبت فقط أن هذه المحاولة المحددة لم تنتج جوابًا قبل حدود عقدها.

> لا يجوز اختصار هذه النتيجة إلى «النموذج المحلي معطّل». كما لا يجوز اعتبار تحميل النموذج أو ظهوره في `ollama ps` نجاحًا لتكامل دعاء؛ معيار النجاح يتطلب جوابًا صحيحًا ومتحققًا ضمن العقد.

## القرار

يبقى محرك دعاء المحلي، ومكتبته DSL الحتمية، المسار التنفيذي الوحيد. لا يعاد تشغيل النموذج تحت عقد V2، ولا ينفذ أي نموذج أوامر أو DSL مباشرة.

أي قرار لاحق يحتاج عقدًا جديدًا منفصلًا، وتحليلًا تقنيًا منفصلًا لزمن بدء هذا النموذج وحجمه ومسار CPU/GPU. لا يتضمن هذا القرار تنزيل نموذج آخر أو تغيير إعدادات Ollama أو Windows أو حذف نموذج قائم.
