# ADR-LLM-0001: تجربة دعاء مع Qwen محلي عبر Ollama

**الحالة:** مقبول للتجربة الضيقة فقط.

## سياق القرار

تم فحص الحاسوب المتصل دون تعديل نظامه. يعمل Windows 11 Home بمعالج Intel i5-13420H وذاكرة مثبتة تقارب 15.64 GiB وبطاقة NVIDIA RTX 4050 Laptop GPU بذاكرة معلنة 4 GiB. يظهر Ollama وLM Studio CLI وPython مثبّتة. لا يوجد نموذج محمّل حاليًا في Ollama.

النماذج المحلية المثبتة تتضمن نماذج أكبر من 7 GiB، ونموذج `qwen3.5:4b` بحجم معروض 3.4 GiB. لذلك يُختار هذا النموذج للتجربة الأولى لأنه موجود بالفعل، والأصغر بين خيارات النماذج العامة المعروضة، ولا يتطلب تنزيلًا. لا يثبت ذلك بعد قدرته على العربية أو توليد برامج صحيحة؛ تلك أمور ستقاس بطلب اصطناعي محدود فقط.

## القرار

تسمح هذه التجربة بطلب محلي واحد أو أكثر ضمن العقد `CONTRACT-LLM-0001` إلى Ollama باستخدام النموذج الدقيق `qwen3.5:4b`. دور النموذج مقصور على **اقتراح** تحويل DSL نصي من مدخل اصطناعي. لا ينفذ دعاء أي برنامج يقترحه النموذج، ولا يعدل مصدره، ولا يثبت أو ينزّل نموذجًا، ولا يرسل بيانات إلى شبكة أو خدمة خارجية.

## الحدود

| جانب | الحد الملزم |
|---|---|
| البيانات | أمثلة اصطناعية فقط؛ لا فواتير أو عملاء أو أسرار حقيقية |
| الشبكة | مغلقة؛ التشغيل المحلي عبر Ollama فقط |
| التنفيذ | حفظ الاستجابة وتقييم بنيتها فقط؛ لا تنفيذ للـDSL المقترح |
| التعديل الذاتي | محظور؛ لا تغيير لملفات دعاء الأساسية أو إعدادات النظام |
| النموذج | `qwen3.5:4b` المثبت حاليًا فقط |
| الدليل | تحفظ المدخلات الاصطناعية والاستجابة والبصمات في `Doaa-Local` |

> لا يمنح هذا القرار النموذج صلاحية أو موثوقية افتراضية. فنجاح تجربة صغيرة لا يثبت أن النموذج صحيح أو آمن أو مناسب لكل المهام.

## معيار الإيقاف

تتوقف التجربة ولا يتخذ قرار تكامل أوسع إذا تعذر بدء النموذج محليًا، أو تجاوز المهلة، أو أظهر طلبًا لشبكة، أو لم ينتج مخرجًا قابلًا للقراءة، أو احتاج أي تعديل خارج العقد.
