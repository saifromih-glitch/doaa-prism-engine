# ADR-LLM-0003: تصحيح عرضي محدود قبل رفض اقتراح النموذج

## القرار

تُضاف مرحلة واحدة فقط قبل الرفض النهائي: تحفظ الاستجابة الخام، ثم تحاول النواة إزالة اختلافات العرض المحددة مسبقًا، وبعدها تعيد التحقق الحتمي. التصحيح المسموح به هو قص الفراغ الخارجي حول الاستجابة الكاملة وإزالة غلاف Markdown واحد مكتمل من نوع JSON. لا يجوز استخراج جزء من النص أو إعادة بناء JSON.

## ما لا يُعد تصحيحًا

لا يجوز إضافة أو حذف أو إعادة تسمية مفتاح، أو تغيير قيمة، أو تخمين نوع المخرج أو العملية أو العمود أو الحجج، أو ترجمة الهدف، أو تحويل نص حر إلى برنامج. لا يجوز للنموذج أو المصحح تنفيذ DSL أو تعديل مصدر أو تشغيل أمر أو الوصول إلى شبكة أو ملف أو سر.

## قاعدة القرار

إذا نجح التحقق الأصلي يُقبل المخرج كاقتراح غير تنفيذي. إذا فشل، تُطبق محاولة التصحيح الوحيدة، ويُسجل معرفها. إذا فشل التحقق بعدها، يُرفض المخرج نهائيًا. لا توجد محاولة ثانية ولا ترقية تلقائية ولا تصحيح صامت.

## سبب الاختيار

هذا التصحيح يعالج تنسيقًا معروفًا لا معنى الطلب. لذلك لا يزيد سلطة النموذج ولا يسمح بتغيير الدلالة. يظل `execution_authority` مساويًا لـ`none`، وتظل كل نتيجة بحاجة إلى مراجعة منفصلة قبل أي تشغيل.

> نجاح التصحيح العرضي لا يعني أن النموذج فهم الطلب؛ يعني فقط أن تمثيلًا مطابقًا للمخطط أصبح قابلًا للفحص.
