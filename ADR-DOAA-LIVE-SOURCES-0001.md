# ADR-DOAA-LIVE-SOURCES-0001

## القرار

تدعم Doaa موصلين صريحين للقراءة فقط: ملخصات Wikimedia من مساري ويكيبيديا العربية والإنجليزية، وبحث مستودعات GitHub العام عبر `api.github.com`. كل طلب شبكة يجب أن يمر عبر host ومسار وقائمة سماح ثابتة.

## دورة البيانات

يعيد الموصل وثيقة مصدر غير موثوقة تحتوي على الرابط النهائي، العنوان، وقت الجلب، وبصمة المحتوى. يمكن تحويل الوثيقة صراحةً إلى سجل أدلة بحالة `pending_review`، ثم يقرر الإنسان اعتمادها عبر `WebEvidenceStore`. لا يعتمد الموصل الأدلة ولا يرقّي المكتبات ولا يستدعي نموذجاً.

## الحماية

تُرفض الروابط غير HTTPS، والمضيفات أو المسارات غير المسموحة، وإعادة التوجيه خارج النطاق، وأنواع المحتوى غير JSON، والردود التي تتجاوز الحد الحجمي. لا تُفسر التعليمات الموجودة داخل المحتوى؛ فهي بيانات غير موثوقة فقط.

## الحدود

هذه الإضافة لا تجعل الويب مصدراً للحقيقة تلقائياً، ولا تضمن صحة المعلومات، ولا تضيف مصادر جديدة ذاتياً. إضافة مصدر أو توسيع مسار أو تغيير سياسة الأدلة يحتاج عقداً واختبارات ومراجعة بشرية منفصلة.
