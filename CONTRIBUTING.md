# Contributing to Doaa

شكرًا لاهتمامك بتطوير Doaa. الهدف هو بناء وسيط لغوي خوارزمي محكوم، وليس وكيلًا مستقلًا.

## قبل إرسال التغيير

اقرأ `README.md` والعقود وقرارات التصميم ذات الصلة. حدّد بوضوح هل التغيير يصلح خطأ، يضيف خوارزمية مسجلة، يوسع التحقق، أو يضيف ناقلًا خارجيًا.

## متطلبات كل تغيير سلوكي

يجب أن يتضمن التغيير اختبارًا حتميًا قابلًا لإعادة التشغيل. إذا أضاف قدرة جديدة، فيجب إضافة عقد مستقل، وحدود تهديد، وحالة رفض آمن، ومراجعة بشرية. لا تقبل النواة التخمين أو التنفيذ التلقائي عند غموض الطلب.

## حدود لا يجوز تجاوزها

لا تضف إلى مسار النواة وصولًا إلى الأسرار أو الشبكة أو أوامر shell أو تعديل المصدر ذاتيًا أو تحديث النموذج أو النشر التلقائي. أي تكامل خارجي يجب أن يبقى محولًا منفصلًا، مع بقاء `execution_authority: "none"` في طبقة الوساطة.

## الاختبار

شغّل:

```powershell
py -3 -m unittest discover -v
```

ثم راجع مخرجات الاختبارات النصية، وتحقق من manifest، وتأكد من عدم وجود ملفات أسرار أو مخرجات تجارب غير مقصودة.

## طلبات الدمج

اشرح المشكلة، نطاق التغيير، الاختبارات التي شغلتها، وأي أثر على البروتوكول أو العقود أو التوكنز. لا تقدّم نتائج تجريبية كضمان عام، واذكر حجم العينة وحدود القياس.
