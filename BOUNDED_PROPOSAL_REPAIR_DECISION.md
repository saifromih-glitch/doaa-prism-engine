# قرار تصحيح الاقتراحات المحدود

## الحالة

تمت إضافة مصحح حتمي مستقل يعمل قبل الرفض النهائي. يحفظ النص الخام، يحاول التحقق الأصلي أولًا، ثم يسمح بتمريرة واحدة فقط لإصلاح اختلافات العرض المعروفة، وبعدها يعيد التحقق عبر البوابة نفسها.

| الاختبار | القرار |
|---|---|
| JSON صحيح دون غلاف | قُبل كاقتراح غير تنفيذي |
| JSON داخل غلاف Markdown مكتمل | صُحح عرضيًا ثم قُبل |
| تغيير العمود إلى عمود غير مسموح | رُفض |
| غلاف Markdown غير مكتمل | رُفض |
| عملية غير معروفة | رُفض |
| تنفيذ DSL أثناء التصحيح | لم يحدث |

نتيجة الاختبارات: `5 passed`، مع `execution_authority: none` و`dsl_execution: false`.

## الضمانات

التصحيح لا يضيف مفاتيح، ولا يحذفها، ولا يعيد تسميتها، ولا يغير القيم، ولا يستنتج العملية أو العمود أو الحجج، ولا يستخرج JSON جزئيًا. ولا يستطيع النموذج أو المصحح الوصول إلى الشبكة أو الملفات أو الأسرار أو مصدر دعاء.

> التصحيح هنا تصحيح تمثيل، لا تصحيح معنى. إذا كانت نية النموذج خاطئة أو العملية غير مسموحة، فالنتيجة رفض نهائي.

## القرار التالي

يمكن الآن ربط المصحح بسير الطلب المحلي V3، مع حفظ سجل يضم النص الخام ومعرف الإصلاح والنص بعد الإصلاح ونتيجة التحقق. يجب إبقاء أي تنفيذ لاحق خارج هذه البوابة وتحت مسار دعاء الحتمي وموافقة صريحة.
