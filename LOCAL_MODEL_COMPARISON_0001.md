# مقارنة مرجعية أولية: Manus مقابل Ollama

**الحالة:** التجربتان غير مقبولتين للتكامل. لا يوجد نموذج مدمج في دعاء نتيجة هذه الدورة.

## نطاق التجربة

استُخدمت بيانات فواتير اصطناعية فقط. لم تُنفذ أي خطوة DSL اقترحها أي نموذج، ولم تتغير ملفات محرك دعاء أو إعدادات Windows، ولم يُنزّل أو يُحذف أي نموذج. كان هدف الاختبار فصل مشكلة النموذج المحلي عن مشكلة صياغة الاختبار، لا إثبات صحة أي نموذج.

| جانب | Ollama المحلي | Manus المرجعي |
|---|---|---|
| النموذج | `qwen3.5:4b` المثبت محليًا | `gpt-5-mini` من القائمة الحية |
| نوع المدخل | بيانات اصطناعية وعقد محدود | البيانات الاصطناعية نفسها وعقد محدود |
| النتيجة | تجاوز مهلة 120 ثانية بلا جواب مسجل | أعاد JSON لكنه خالف العملية والعمود المطلوبين |
| تنفيذ DSL | لم يحدث | لم يحدث |
| قرار القبول | مرفوض | مرفوض |

## الأدلة

تجاوز اختبار Ollama مهلة العقد الأولية بينما كانت الذاكرة الحرة تقارب 0.31 GiB وكانت عملية `llama-server` تستهلك نحو 5.6 GiB. بعد توقف العملية، ظهر أن الذاكرة الحرة تقارب 7.51 GiB ولم تظهر نماذج نشطة في `ollama ps`. هذا لا يثبت أن Ollama أو النموذج معطّل؛ بل يثبت فقط أن الاختبار الأول لم ينتج جوابًا ضمن المهلة وتحت ظروف الموارد حينها.

أعاد اختبار Manus استجابة JSON قابلة للقراءة، لكنه اقترح عملية غير مسموحة باسم يبدأ بـ`load_synthetic_rows` واستخدم العمود `_source` بدل `remove_ascii_phone_separators` على `phone`. لذلك رفضه المتحقق الحتمي. لا يثبت ذلك عطل نموذج Manus؛ يثبت أن هذه الاستجابة المحددة لا تفي بالعقد الدقيق حتى مع طلب مخرج مقيد.

> النتيجة الأهم: لا يجوز تنفيذ اقتراحات النماذج مباشرة. يجب أن تظل طبقة التحقق الحتمية في دعاء هي الجهة الوحيدة التي تقبل أو ترفض برنامج DSL.

## القرار الحالي

يبقى دعاء في وضعه الآمن الحالي: مكتبة DSL المحلية المستقرة هي المسار التنفيذي الوحيد. لا يعاد اختبار Ollama تحت `CONTRACT-LLM-0001` لأنه انتهى بالمهلة، ولا يتسع اختبار Manus تلقائيًا بسبب استجابة واحدة غير مطابقة.

أي تجربة لاحقة تحتاج عقدًا جديدًا منفصلًا يحدد، قبل التنفيذ، أحد الخيارين فقط: اختبار Ollama بعد تحرير الموارد بمهلة بدء أطول ومخرج أبسط، أو اختبار Manus بمثال خال من تمثيل الصفوف داخل النص ومع تحقق صارم مستقل. لا تتضمن هذه الوثيقة موافقة على تنفيذ أي من الخيارين.
